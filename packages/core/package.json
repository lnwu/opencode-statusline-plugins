{
  "name": "core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Internal shared code for the statusline plugins: language resolution, the e2e harness, and the build helper. Never published.",
  "exports": {
    "./language": "./src/language.ts",
    "./theme": "./src/theme.ts",
    "./harness": "./src/harness.ts",
    "./record-demo": "./src/record-demo.ts",
    "./build": "./src/build.ts",
    "./pack": "./src/pack.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@opentui/solid": "^0.5.11",
    "bun-types": "^1.4.2",
    "typescript": "^7.0.2"
  }
}
